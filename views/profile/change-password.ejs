<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= hasPassword ? 'Change Password' : 'Set Password' %> - AlisterBank</title>
    <link rel="stylesheet" href="/style.css" />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <%- include('../header') %>

    <main>
      <section class="profile-section">
        <div class="section-container">
          <div class="back-link">
            <a href="/profile">
              <svg
                width="20"
                height="20"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M15 19l-7-7 7-7"
                />
              </svg>
              Back to Profile
            </a>
          </div>

          <div class="profile-content">
            <div class="profile-card profile-card-centered">
              <div class="card-header-icon">
                <svg
                  width="48"
                  height="48"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"
                  />
                </svg>
              </div>
              <h2><%= hasPassword ? 'Change Password' : 'Set Password' %></h2>
              <p class="card-description">
                <% if (hasPassword) { %>
                Enter your current password and choose a new password to update
                your account security.
                <% } else { %>
                Set a password for your account to enable login with email and password.
                <% } %>
              </p>

              <form action="/change-password" method="POST">
                <% if (hasPassword) { %>
                <div class="input-group">
                  <label for="currentPassword">Current Password</label>
                  <div class="password-input-wrapper">
                    <input
                      type="password"
                      id="currentPassword"
                      name="currentPassword"
                      placeholder="Enter current password"
                      required
                    />
                    <button
                      type="button"
                      class="password-toggle"
                      onclick="togglePasswordVisibility('currentPassword')"
                    >
                      <svg
                        class="eye-icon"
                        width="20"
                        height="20"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                      >
                        <path
                          stroke-linecap="round"
                          stroke-linejoin="round"
                          stroke-width="2"
                          d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"
                        />
                        <path
                          stroke-linecap="round"
                          stroke-linejoin="round"
                          stroke-width="2"
                          d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"
                        />
                      </svg>
                    </button>
                  </div>
                </div>
                <% } %>

                <div class="input-group">
                  <label for="newPassword"><%= hasPassword ? 'New Password' : 'Password' %></label>
                  <div class="password-input-wrapper">
                    <input
                      type="password"
                      id="newPassword"
                      name="newPassword"
                      placeholder="<%= hasPassword ? 'Enter new password' : 'Enter password' %>"
                      required
                    />
                    <button
                      type="button"
                      class="password-toggle"
                      onclick="togglePasswordVisibility('newPassword')"
                    >
                      <svg
                        class="eye-icon"
                        width="20"
                        height="20"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                      >
                        <path
                          stroke-linecap="round"
                          stroke-linejoin="round"
                          stroke-width="2"
                          d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"
                        />
                        <path
                          stroke-linecap="round"
                          stroke-linejoin="round"
                          stroke-width="2"
                          d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"
                        />
                      </svg>
                    </button>
                  </div>
                </div>

                <div class="input-group">
                  <label for="confirmNewPassword"><%= hasPassword ? 'Confirm New Password' : 'Confirm Password' %></label>
                  <div class="password-input-wrapper">
                    <input
                      type="password"
                      id="confirmNewPassword"
                      name="confirmNewPassword"
                      placeholder="<%= hasPassword ? 'Confirm new password' : 'Confirm password' %>"
                      required
                    />
                    <button
                      type="button"
                      class="password-toggle"
                      onclick="togglePasswordVisibility('confirmNewPassword')"
                    >
                      <svg
                        class="eye-icon"
                        width="20"
                        height="20"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                      >
                        <path
                          stroke-linecap="round"
                          stroke-linejoin="round"
                          stroke-width="2"
                          d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"
                        />
                        <path
                          stroke-linecap="round"
                          stroke-linejoin="round"
                          stroke-width="2"
                          d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"
                        />
                      </svg>
                    </button>
                  </div>
                </div>

                <% if (error && error.length > 0) { %>
                <div class="verification-message error"><%= error[0] %></div>
                <% } %> <% if (success && success.length > 0) { %>
                <div class="verification-message success">
                  <%= success[0] %>
                </div>
                <% } %>
                <div class="form-actions">
                  <button type="submit" class="btn-primary">
                    <%= hasPassword ? 'Update Password' : 'Set Password' %>
                  </button>
                  <a href="/profile" class="btn-outline">Cancel</a>
                </div>
              </form>
            </div>
          </div>
        </div>
      </section>
    </main>

    <%- include('../footer') %>
  </body>
</html>
